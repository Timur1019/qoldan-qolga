# Прямые запросы на бэкенд (без прокси). Бэкенд должен быть запущен: ./gradlew bootRun
# CORS уже настроен для localhost:3000. Пусто = прокси через Vite на :8080.
VITE_API_ORIGIN=http://localhost:8080
